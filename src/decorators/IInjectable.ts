import Context from "../core/Context";

/**
 * Class interface that should be implemeted to support the Injectable class dectorator
 * @author  Neil Rackett
 */
export interface IInjectable {
  context: Context;
}